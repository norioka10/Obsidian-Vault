---
type: guide
created: 2026-04-12
updated: 2026-06-10
tags: [運用ガイド]
---

# Vault運用ガイド（AI向け）

> Claude / Codex / Cursor など、どのAIツールからこのVaultを触る場合も本ガイドに従う。
> 元の指示（2026-04-12）: 「各ソースの要約を書いて、主要な概念をリストアップして、マスターインデックスを作って」

## 構造と参照順序

1. [[wiki/hot]] — 最初に読む（頻用概念・最近の関心・Vault概要）
2. [[wiki/index]] — 全ページの唯一の正（テーマ別・50音順・追加履歴）
3. [[概念/概念インデックス]] — 書籍横断の概念ハブ
4. 各書籍ページ（`wiki/*.md`）— 詳細

`マスターインデックス.md` と `MOC/` は人間用の眺める地図。AIは index.md を正とする。

## 新しい本を取り込むときの標準フォーマット

- ファイル名: `{書名} 要約＆概念マップ.md`（`＆`は全角。場所は `wiki/` 直下）
- frontmatter（必須）:
  ```yaml
  ---
  type: book-summary
  created: YYYY-MM-DD
  author: "著者名"
  tags: [読書, テーマタグ...]
  ---
  ```
- 本文構成: H1見出し → メタ情報（原典ノート/原典PDF/作成日）→ 書籍概要 → 全体構成表 → 章別要約 → 主要概念一覧 → 実務アクション → 関連ノート（双方向リンク）→ 末尾タグ行
- 取り込み後に**必ず**やること:
  1. `wiki/index.md` を更新（ページ数・テーマ別・全ページ一覧・追加履歴）
  2. `wiki/log.md` に1行追記（日時＋内容）
  3. 既存ページとの双方向リンクを最低2本張る
  4. 3冊以上で登場する概念が出たら `概念/` にハブノートを検討

## タグ運用ルール

- frontmatter `tags` と末尾タグ行は同期させる（正はfrontmatter）
- 全ページ共通: `読書`。テーマタグは index.md のテーマ別分類と一致させる
- `#SCM` は供給網の戦略・設計、`#物流` は輸配送・倉庫・法対応など実務運用に付与

## 概念ハブノート（`概念/`）のルール

- 追加基準: 3冊以上に登場する概念、または実務で繰り返し参照する概念
- 構成: 定義（3行以内）／登場する書籍／実務での適用／関連概念
- 追加したら [[概念/概念インデックス]] と [[wiki/hot]] に反映

## 還元ループ

- AIとの会話で出た良い洞察は `/save` でWikiに保存する
- 質問応答時は根拠ページを `[[wikilink]]` で添える
- 月1回程度 wiki-lint（デッドリンク・孤立ページ・index数の不一致チェック）を実行する

## 禁止事項

- `raw/` は読み取り専用（原典の全冊管理はKindle PDFが正。raw/ は初期20冊のみ）
- index.md と実ページ数の不一致を放置しない
- 同一書籍の重複ノートを作らない（表記ゆれに注意。作成前に必ず index.md で確認）
